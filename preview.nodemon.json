{
  "exec": "pwsh.exe --Command \"cls;if (Test-Path ./.next) { Remove-Item -Path ./.next -Recurse -Force }\" && yarn build && yarn start",
  "ignore": [
    "./next",
    "./node_modules"
  ],
  "watch": [
    "next.config.json",
    "preview.nodemon.json",
    "./pages/*.(tsx?|jsx?)"
  ],
  "$schema": "https://json.schemastore.org/nodemon.json",
  "cwd": "."
}